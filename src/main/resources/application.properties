# Mailtrap (sandbox) SMTP configuration
# Host: sandbox.smtp.mailtrap.io
# Allowed ports: 25, 465, 587 or 2525 — 2525 is commonly used and less likely to be blocked.
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=6795143f3f342e
spring.mail.password=89d06af0f46e59
spring.mail.protocol=smtp

# SMTP properties
spring.mail.properties.mail.smtp.auth=true
# STARTTLS is optional in Mailtrap; enable for ports that support it (587, 2525)
spring.mail.properties.mail.smtp.starttls.enable=true
# If you prefer using SSL (port 465), enable the following instead:
spring.mail.properties.mail.smtp.ssl.enable=false

# Optional default from address used by the service
spring.mail.from=sender@example.com